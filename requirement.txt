# ICD10 Code Blue dependencies
#
# This project currently uses only Python standard library modules:
# tkinter, csv, pathlib, datetime, dataclasses, enum, math, random.
#
# Keep this file for assignment structure compatibility.
 